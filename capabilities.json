{
    "version": "1.0",
    "name": "Optimus",
    "description": "Ingests tracker positions from Optimus (Optimus Pro) and posts them to the map.",
    "compute": {
        "memory": 512,
        "timeout": 60
    },
    "permissions": [
        {
            "resource": "feature:*",
            "required": true,
            "description": "Optimus posts tracker positions to the map"
        }
    ],
    "invocations": {
        "incoming": {
            "schedule": {
                "description": "Polls Optimus for updated tracker positions on a schedule.",
                "default": {
                    "enabled": true,
                    "schedule": "rate(1 minute)"
                }
            }
        }
    }
}
